
# GitHub 面板 Pro+ 插件系统免责声明

**版本 1.0 | 生效日期：2026.6.7**

---

## 1. 一般说明

GitHub 面板 Pro+（以下简称“本扩展”）的插件系统（以下简称“插件系统”）是一种开放、动态的代码执行机制。任何第三方、本扩展作者或用户提供的插件代码，均以 **“按原样”（AS IS）** 的方式提供给用户使用。本扩展的作者、贡献者、分发者（以下统称“本扩展提供方”）**不对任何插件的内容、安全性、稳定性、合法性、准确性或适用性作任何明示或默示的保证**。

---

## 2. 高风险与完全授权

插件的运行环境具有 **完全且不受限制的权限**，具体包括但不限于：

- 读取、修改、删除本扩展存储的所有数据，包括 **GitHub 个人访问令牌（Token）**；
- 通过 GitHub API 以用户身份 **创建、修改、删除任何仓库、文件、分支、Pull Request、Workflow**；
- 读取系统的 **localStorage、剪贴板、浏览器存储**；
- 控制并修改 **本扩展的所有 UI 元素、内部对象、全局变量、DOM**；
- 发起 **任意 HTTP 请求**，可能将用户数据（包括 Token）传输至第三方服务器；
- 加载并执行 **额外的脚本、代码或资源**。

**使用插件系统即表示用户完全知晓并自愿承担上述风险。**

---

## 3. 无担保与责任限制

在法律允许的最大范围内，本扩展提供方 **不承担任何因使用或无法使用插件系统而导致的直接、间接、偶然、特殊、惩罚性或衍生性损害赔偿责任**，包括但不限于：

- 数据丢失或泄露；
- GitHub 账号被封禁或受到限制；
- 仓库、代码或内容被意外删除；
- 第三方通过插件获取用户 Token 并进行恶意操作；
- 系统性能下降、崩溃或兼容性问题；
- 任何形式的财务损失或名誉损害。

即使本扩展提供方已被告知可能发生此类损害，亦不承担责任。

---

## 4. 用户责任

用户在使用插件系统时，**应当并且必须**：

- 仅加载 **来源可靠、代码可读、经过审查** 的插件；
- 在加载任何外部插件前，**自行阅读并理解其完整源代码**；
- 使用 **最小权限 GitHub Token**（建议使用 Fine-Grained Token 并仅授予必要仓库和权限）；
- 定期更换 Token，并不将 Token 用于本扩展以外的其他服务；
- 如发现插件存在恶意行为，立即停止使用并通过 `pluginManager.unloadPlugin(id)` 卸载该插件，并撤销 Token；
- 对因使用插件而产生的所有后果 **独立承担全部责任**。

---

## 5. 插件市场的特别说明

本扩展的“插件集市”功能所展示的插件 **由社区用户或第三方维护者提供**，本扩展提供方 **未对插件集市中的任何插件进行安全性审查、代码审计或功能验证**。插件集市仅作为一个技术性的目录聚合与加载工具，**不构成推荐、背书或担保**。

插件集市仓库的维护者对其仓库内容负全部责任。本扩展提供方 **不对插件集市中插件的任何缺陷、恶意行为、兼容性问题或数据安全事件负责**。

---

## 6. 第三方代码与依赖

插件可能引入或依赖第三方库、服务、API 或资源。本扩展提供方 **不对任何第三方代码或服务的合法性、安全性、可用性负责**，也不对因第三方原因导致的任何损失承担责任。

---

## 7. 用户同意

通过加载、安装、运行或以任何方式使用本插件系统中的任何插件，即表示用户：

- 已 **阅读、理解并同意** 本免责声明的全部条款；
- 自愿承担使用插件系统的所有风险；
- 放弃就插件系统相关事宜向本扩展提供方提出任何索赔或诉讼的权利（法律不可排除的责任除外）。

---

## 8. 法律适用与争议解决

本免责声明受 **中华人民共和国法律** 管辖并依其解释。因本声明引起的或与之有关的任何争议，双方应首先友好协商解决；协商不成的，任何一方均可将争议提交至 **本扩展提供方所在地有管辖权的人民法院** 诉讼解决。

---

## 9. 可分割性

如本免责声明中的任何条款被认定为无效、非法或不可执行，其余条款的效力不受影响，仍对双方具有约束力。

---

## 10. 更新与修改

本扩展提供方保留随时修改、更新或替换本免责声明的权利，修改后的声明自发布之日起生效。重大变更将通过扩展界面或插件集市公告等方式通知用户。用户继续使用插件系统即表示接受修改后的声明。

---

**© GitHub 面板 Pro+ 项目组**  
**最后更新：2026年6月7日 **